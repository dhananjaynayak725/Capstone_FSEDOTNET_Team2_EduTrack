# Leave empty to use the Vite dev proxy (recommended). Set to the API origin to call it directly,
# e.g. http://localhost:5080 (the API's CORS policy allows http://localhost:5173).
VITE_API_BASE_URL=
